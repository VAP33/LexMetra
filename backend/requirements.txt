# Core API
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
python-multipart>=0.0.9,<1.0

# Computer vision / image processing
opencv-python-headless>=4.9,<5.0
numpy>=1.26,<3.0
Pillow>=10.0,<12.0
imagehash>=4.3,<5.0

# HTTP / API testing
httpx>=0.27,<1.0

# Database
psycopg2-binary>=2.9,<3.0

# Configuration / secrets
python-dotenv>=1.0,<2.0

# Authentication / RBAC
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
bcrypt>=4.0,<5.0

# PDF report generation
reportlab>=4.0,<5.0

# OCR
# pytesseract is a thin Python wrapper. It ALSO requires the Tesseract OCR
# binary to be installed on the host/container (it is NOT a pip package):
#   Debian/Ubuntu: sudo apt-get install -y tesseract-ocr tesseract-ocr-eng
#   macOS (brew):  brew install tesseract
#   Windows:       https://github.com/UB-Mannheim/tesseract/wiki
# ocr_extraction.py degrades gracefully (returns no OCR lines, everything
# evaluates to UNCERTAIN/NOT_OBSERVED) if the binary is missing, but OCR
# extraction will not function without it.
pytesseract>=0.3.10,<1.0

# OCR ensemble: Tesseract is the baseline; PaddleOCR is the complementary
# scene-text engine used by the demo when its runtime is available.
paddleocr>=3.1,<4.0
paddlepaddle>=3.0,<4.0

# Test suite
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# Optional VLM Provider (Gemini)
google-generativeai>=0.8.0

